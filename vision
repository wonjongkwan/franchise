import { Card, CardContent } from "@/components/ui/card";
import { motion } from "framer-motion";

export default function FnbAIEcosystemPost() {
  const slides = [
    {
      title: "Together, we build the Restaurant AI Ecosystem",
      subtitle: "함께 만들어가는 F&B AI 생태계",
      emoji: "🌐",
      bg: "bg-gradient-to-br from-orange-100 to-yellow-50",
    },
    {
      title: "AI usability era begins – OpenAI Dev Day 2025",
      text: "이제 AI는 연구의 시대를 넘어, 누구나 쉽게 쓰는 시대로 들어섰습니다. 도구와 에이전트, 플랫폼이 하나의 생태계로 연결되고 있습니다.",
      emoji: "🧠",
      bg: "bg-gradient-to-br from-blue-50 to-cyan-100",
    },
    {
      title: "레스토랑 운영의 모든 OS가 연결되는 시대",
      text: "POS, 배달, 마케팅, 인사, 재고 등 매장의 모든 운영요소가 하나의 OS로 통합되고, AI가 사장님의 의사결정을 돕는 구조로 진화합니다.",
      emoji: "🍳",
      bg: "bg-gradient-to-br from-amber-50 to-orange-100",
    },
    {
      title: "Platformized. Usable. Domain-Intelligent.",
      text: "플랫폼화된 연결 구조, 사용성을 통한 해자 구축, 사장님의 질문과 데이터가 곧 도메인지식이 되는 구조.",
      emoji: "🔗",
      bg: "bg-gradient-to-br from-gray-50 to-slate-100",
    },
    {
      title: "From Korea, for the world.",
      text: "외식업의 혁신은 혼자서 이룰 수 없습니다. F&B 버티컬 스타트업들이 서로 연결되고, 데이터를 공유하며 생태계를 만들어간다면 한국에서 시작된 F&B 혁신이 세계로 나아갈 것입니다.",
      emoji: "🌏",
      bg: "bg-gradient-to-br from-green-50 to-emerald-100",
    },
    {
      title: "기술이 사장님을 돕고, 사장님이 식탁의 가치를 만드는 세상.",
      text: "그 변화를 함께 만들어가고 싶습니다.",
      emoji: "✨",
      bg: "bg-gradient-to-br from-yellow-50 to-orange-100",
    },
  ];

  return (
    <div className="grid gap-6 p-8 max-w-3xl mx-auto">
      {slides.map((s, i) => (
        <motion.div
          key={i}
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: i * 0.15 }}
        >
          <Card className={`${s.bg} rounded-2xl shadow-md p-6`}> 
            <CardContent>
              <div className="text-4xl mb-3">{s.emoji}</div>
              <h2 className="text-xl font-semibold mb-2 text-gray-800">{s.title}</h2>
              {s.subtitle && (
                <p className="text-lg font-medium text-orange-700 mb-2">{s.subtitle}</p>
              )}
              {s.text && (
                <p className="text-gray-600 leading-relaxed text-base">{s.text}</p>
              )}
            </CardContent>
          </Card>
        </motion.div>
      ))}
      <div className="text-center mt-6 text-gray-500 text-sm">
        #AI #OpenAI #DevDay #VerticalAI #F&B혁신 #레스토지니 #ControlM #RestaurantOS #Ecosystem #KoreanStartups #TogetherWeBuild
      </div>
    </div>
  );
}
